version: 0.2

#env:
  #variables:
      #db_endpoint: "value"
      #db_user: "value"
      #db_password: ""
  #parameter-store:
     # key: "value"
     # key: "value"

phases:
  #install:
    #commands:
      # - command
      # - command
  #pre_build:
    #commands:
      # - command
      # - command
  build:
    commands:
       - ./scripts/build.sh
  #post_build:
    #commands:
      # - command
      # - command
artifacts:
  files:
    - '**/*'
    # - location
  name: my_php_app_$(date +%Y-%m-%d)
  #discard-paths: yes
  #base-directory: location
#cache:
  #paths:
    # - paths
